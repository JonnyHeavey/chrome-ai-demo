import { WriterOptions } from '../../../shared/types/chrome-ai-apis.types';

export const DEFAULT_WRITER_OPTIONS: WriterOptions = {
  tone: 'neutral',
  format: 'markdown',
  length: 'medium',
  sharedContext: '',
};
